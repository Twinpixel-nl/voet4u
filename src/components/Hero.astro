---
import { Image } from 'astro:assets';
import heroImage from '../assets/foto1.jpeg';
---
<section class="relative min-h-[80vh] flex items-center bg-white pt-24 pb-16">
  <div class="container mx-auto px-6">
    <div class="flex flex-col lg:flex-row items-center gap-16 lg:gap-24">
      
      <!-- Tekstgedeelte -->
      <div class="w-full lg:w-1/2 text-center lg:text-left order-2 lg:order-1">
        <div class="inline-flex items-center gap-3 mb-8">
          <span class="h-[1px] w-8 bg-[#8fb3c5]"></span>
          <span class="text-[#8fb3c5] uppercase tracking-[0.4em] text-[10px] font-bold">
            Praktijk Voet 4 U
          </span>
        </div>

        <h1 class="text-5xl lg:text-8xl font-serif text-[#2c3e50] leading-[1.1] mb-8">
          Vind je natuurlijke <br/>
          <span class="italic text-[#4a7c92] relative inline-block">
            balans
            <!-- Subtiel handgetekend lijntje -->
            <svg class="absolute -bottom-2 left-0 w-full" viewBox="0 0 200 10" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M2 8C50 3 150 3 198 8" stroke="#8fb3c5" stroke-width="2" stroke-linecap="round"/>
            </svg>
          </span>.
        </h1>

        <p class="text-lg text-slate-500 font-light leading-relaxed mb-12 max-w-lg mx-auto lg:mx-0">
          Gespecialiseerde voetreflexmassage, oor-acupunctuur en cupping in Randwijk. Een holistische aanpak voor een vitaal lichaam en een rustige geest.
        </p>

        <div class="flex flex-col sm:flex-row items-center justify-center lg:justify-start gap-8">
          <a href="#contact" class="px-12 py-4 bg-[#4a7c92] text-white rounded-full hover:bg-[#3d6679] transition-all shadow-xl shadow-blue-900/10 uppercase tracking-[0.2em] text-[10px] font-bold">
            Maak een afspraak
          </a>
          <a href="#behandelingen" class="group flex items-center gap-2 text-[10px] uppercase tracking-[0.2em] font-bold text-slate-400 hover:text-[#4a7c92] transition-colors">
            Bekijk behandelingen
            <svg class="w-4 h-4 transform group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
          </a>
        </div>
      </div>

      <!-- Afbeeldinggedeelte -->
      <div class="w-full lg:w-1/2 order-1 lg:order-2">
        <div class="relative">
          <!-- De elegante boogvorm -->
          <div class="relative z-10 rounded-t-full rounded-b-2xl overflow-hidden shadow-[0_32px_64px_-16px_rgba(0,0,0,0.1)] border-[10px] border-white">
            <Image 
              src={heroImage} 
              alt="Anja Jonker - Voet 4 U" 
              class="w-full h-[500px] lg:h-[600px] object-cover"
            />
          </div>
          
          <!-- Abstract accentje: Alleen een dunne cirkel ipv een blob -->
          <div class="absolute -top-10 -right-10 w-40 h-40 border border-blue-50 rounded-full -z-0"></div>
          <div class="absolute -bottom-10 -left-10 w-64 h-64 border border-blue-50 rounded-full -z-0"></div>

          <!-- Kleine zwevende info-badge -->
          <div class="absolute bottom-12 -left-8 z-20 bg-white/90 backdrop-blur px-6 py-4 rounded-2xl shadow-lg border border-blue-50 hidden md:block">
            <p class="text-[10px] font-bold text-[#8fb3c5] uppercase tracking-widest mb-1">Specialisme</p>
            <p class="text-sm font-serif italic text-slate-700">Holistische zorg & aandacht</p>
          </div>
        </div>
      </div>

    </div>
  </div>
</section>