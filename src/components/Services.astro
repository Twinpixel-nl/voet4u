<section id="diensten" class="py-20">
  <div class="container mx-auto px-6 text-center">
    <h2 class="text-3xl font-bold text-blue-900">Onze Behandelingen</h2>
    <div class="mt-12 grid md:grid-cols-3 gap-8">
      {['Basis Pedicure', 'Voetreflexmassage', 'Oor-acupunctuur'].map((service) => (
        <div class="p-8 bg-white border border-blue-50 rounded-2xl shadow-sm hover:shadow-md transition">
          <div class="w-12 h-12 bg-blue-100 rounded-full flex items-center justify-center mb-6 mx-auto">
            <div class="w-6 h-6 bg-blue-500 rounded-full"></div>
          </div>
          <h3 class="text-xl font-bold mb-4">{service}</h3>
          <p class="text-slate-600 text-sm italic">Vanaf €32,50</p>
        </div>
      ))}
    </div>
  </div>
</section>